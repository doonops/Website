{
  "name": "doonops-next",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "next dev",
    "dev:clean": "rm -rf .next && next dev",
    "build": "next build",
    "build:docs": "npm run build && node scripts/sync-docs.mjs",
    "start": "next start",
    "preview": "npx serve out",
    "generate:bootcamp": "node scripts/parse-python-bootcamp.mjs",
    "generate:terraform": "node scripts/generate-terraform-course.mjs"
  },
  "dependencies": {
    "next": "^14.2.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/react": "^18.2.66",
    "@types/node": "^20.12.7"
  }
}
